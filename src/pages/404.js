import * as React from "react"

// styles
const pageStyles = {
  color: "#FFFFFF",
  padding: "96px"
}

// markup
const NotFoundPage = () => {
  return (
    <main style={pageStyles}>
      <title>Not found</title>
      <h1>The Dad you are looking for is not found. Please try another guy.</h1>
    </main>
  )
}

export default NotFoundPage
